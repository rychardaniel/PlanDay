version: '3.8'

services:
  plandayapi:
    image: plandayapi
    build:
      context: .
      dockerfile: PlanDayAPI/Dockerfile

  postgres:
    image: postgres:latest
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: PlanDayDB
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: